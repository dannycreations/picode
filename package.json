{
  "name": "pi-code-workspace",
  "version": "0.0.0",
  "author": "dannycreations",
  "license": "GPL",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=24.13.3"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "check": "pnpm run format && tsc --noEmit && pnpm -r run check",
    "format": "prettier --write .",
    "test": "pnpm -r run test",
    "setup": "tsx scripts/setup.ts",
    "update": "npm-check-updates -u -x @types/node --workspaces --root",
    "prepare": "husky"
  },
  "devDependencies": {
    "@ianvs/prettier-plugin-sort-imports": "^4.7.1",
    "@types/node": "24.13.3",
    "husky": "^9.1.7",
    "npm-check-updates": "^23.0.2",
    "prettier": "^3.9.6",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2"
  },
  "packageManager": "pnpm@11.21.0"
}
